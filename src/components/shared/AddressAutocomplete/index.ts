/**
 * Fachada pública del componente AddressAutocomplete.
 *
 * Los consumidores importan SIEMPRE desde aquí, nunca desde archivos
 * internos, para que podamos reorganizar la carpeta sin romper imports.
 */
export { AddressAutocomplete } from './AddressAutocomplete';
export type { AddressAutocompleteProps, AddressSelection } from './AddressAutocomplete.types';
