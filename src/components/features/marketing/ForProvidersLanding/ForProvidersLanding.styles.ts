/**
 * Estilos del orquestador ForProvidersLanding.
 *
 * Sin estilos propios: cada sección hija aporta su `bg-*` y padding.
 * Mantenemos el contenedor como `contents` para no introducir un
 * elemento de layout adicional en el árbol.
 */
export const forProvidersLandingStyles = {
  root: 'contents',
} as const;
